//! Criterion benchmark: zero-copy transform vs. naive full-deserialize baseline.
//! Populated in full at Step 12; this stub keeps `cargo build`/`cargo bench` wired
//! from Step 1 onward.

fn main() {}
